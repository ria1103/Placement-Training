#include<bits/stdc++.h>
using namespace std;
int main()
{
    int a,n,rem,sum=0;
    cin>>a;
    n=a;
    while(n>0){
        rem=n%10;
        sum+=pow(rem,3);
        n=n/10;
    }
    if(sum==a)
        cout<<"Yes";
    else
        cout<<"No";
    cout<<endl;
}
