#include<bits/stdc++.h>
using namespace std;
int main(){
    int n,k,score,cnt=0;
    int a[50];
    cin>>n>>k;
    for(int i=0; i<n; i++){
        cin>>a[i];
    }
    score = a[k-1];
    for(int i=0; i<n; i++){
        if(a[i] >= score && a[i]>0)
        cnt++;
    }
    cout<<cnt<<endl;
}
